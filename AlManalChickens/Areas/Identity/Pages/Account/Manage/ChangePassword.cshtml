@page
@model ChangePasswordModel
@{
    ViewData["Title"] = "تعديل كلمة المرور";
    Layout = "_Layout2";
}

<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

<style>
    .far.fa-eye {
        font-size: medium;
        top: 40px;
        height: 16px;
        position: absolute;
        left: 12px;
        width: 16px;
    }

    .form-group {
        position: relative
    }
</style>

<h2>@ViewData["Title"]</h2>

<form id="change-password-form" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <label asp-for="Input.OldPassword">كلمة المرور القديمة</label>
        <input asp-for="Input.OldPassword" class="form-control" />
        <span asp-validation-for="Input.OldPassword" class="text-danger"></span>
        <i class="far fa-eye"></i>
    </div>
    <div class="form-group">
        <label asp-for="Input.NewPassword">كلمة المرور الجديدة</label>
        <input asp-for="Input.NewPassword" class="form-control" />
        <span asp-validation-for="Input.NewPassword" class="text-danger"></span>
        <i class="far fa-eye"></i>
    </div>
    <div class="form-group">
        <label asp-for="Input.ConfirmPassword">تاكيد كلمة المرور الجديدة</label>
        <input asp-for="Input.ConfirmPassword" class="form-control" />
        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
        <i class="far fa-eye"></i>
    </div>
    <div class="form-group">
        <button type="submit" class="btn-new-style">تغيير كلمة المرور</button>
    </div>
</form>

<div class="form-group">
    <a asp-action="Index" asp-controller="Home" class="btn-new-style">العودة الى الرئيسية</a>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(".fa-eye").on("click", function () {
            $(this).toggleClass("active");
            if ($(this).hasClass("active")) {
                $(this).parent(".form-group").find("input").attr({ "type": "text" });
            } else {
                $(this).parent(".form-group").find("input").attr({ "type": "password" });
            }
        });

    </script>
}
